---
import { SITE_DATE_LOCALE } from '../consts';

interface Props {
date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
{
date.toLocaleDateString(SITE_DATE_LOCALE, {
year: 'numeric',
month: '2-digit',
day: '2-digit',
})
}
</time>
